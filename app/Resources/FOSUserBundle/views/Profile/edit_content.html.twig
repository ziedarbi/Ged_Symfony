{% trans_default_domain 'FOSUserBundle' %}
{#
{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'fos_user_profile_edit' } }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
{{ form_end(form) }}
#} 
<div class="col-sm-9 col-sm-offset-1  col-md-6 col-md-offset-3" style="background-color:#dde7ff;">
    <h2 align="center"> Formulaire d'inscription </h2>
    {{ form_start(form, {'method': 'post', 'action': path('fos_user_profile_edit'), 'attr': {'class': 'fos_user_registration_register'}}) }}
    {{ form_row(form.username) }}
    {{ form_row(form.email) }}

   {# {{ form_row(form.roles) }}  #}
    <div align="center">
        <p></p>
        <input type="submit"  class="btn btn-primary" value="Enregistrer" />
        <p> Si vous avez un compte cliquez sur <a href="{{ path('fos_user_security_login') }}">Connexion</a> </p>
    </div>

</div>
{{ form_end(form) }}