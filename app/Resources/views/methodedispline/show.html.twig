{% extends 'base.html.twig' %}

{% block Maincontent %}
<section class="invoice">
    <h1>Methodedispline</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ methodeDispline.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ methodeDispline.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('methodedispline_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('methodedispline_edit', { 'id': methodeDispline.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
</section>

{% endblock %}
